#[derive(Debug)]
pub struct Matrix {
    rows: u32,
    columns: u32,
    matrix: Vec<Vec<T>>,
}

impl Matrix {
    pub fn new(rows: u32, columns: u32) -> Matrix {
        Matrix {
            rows: rows,
            columns: columns,
            matrix: ,
        }
    }
}